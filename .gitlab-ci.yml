---

variables:
  CI_PYTHON_UNIVERSAL: "yes"
  CI_SKIP_STATIC_ANALYSIS: "yes"

include:
  - project: 'dpres/gitlab-ci-pipeline'
    ref: master
    file:
      - '/pipeline/common.yml'
      - '/pipeline/fddps.yml'
      - '/pipeline/kppas.yml'
      - '/pipeline/sapa.yml'
